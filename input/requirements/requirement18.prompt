JSON Schema Reuse: Act as a senior programmer analyzing the code in a TypeScript application where the content of each file in the application's codebase is shown below in a code block. Identify places where the same JSON schema is defined in multilpe places and should be instead a shared schema should be used. Identify how and where in the codebase the schema should be shared. For example, some templated LLM prompts use the same schema as some of the repositories. Provide references to the specific part(s) of the code that needs these improvements with suggestions on how to improve. Include the list or relative file paths of each source file affected.